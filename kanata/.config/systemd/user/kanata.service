[Unit]
Description=Kanata keyboard remapper
After=graphical-session.target
PartOf=graphical-session.target

[Service]
Type=simple
ExecStart=/usr/bin/kanata -n --no-wait -c %h/.config/kanata/config.kbd
Restart=on-failure
RestartSec=1

[Install]
WantedBy=graphical-session.target
